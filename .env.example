# Example environment configuration for Drizzle + PostgreSQL

# Environment type (development | production)
NODE_ENV=development

# Connection string for local PostgreSQL
DATABASE_URL=postgres://<USERNAME>:<PASSWORD>@<HOST>:<PORT>/<DATABASE_NAME>

# Optional individual PostgreSQL variables (if needed)
POSTGRES_DB=<DATABASE_NAME>
POSTGRES_USER=<USERNAME>
POSTGRES_PASSWORD=<PASSWORD>

# Example
# DATABASE_URL=postgres://postgres:admin@localhost:5432/mydb
